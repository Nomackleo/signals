import { CustomLevelDirective } from './custom-level.directive';

describe('CustomLevelDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomLevelDirective();
    expect(directive).toBeTruthy();
  });
});
